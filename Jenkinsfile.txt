pipeline {
    agent any
    tools {
        maven 'Maven3' // Use your configured Maven tool name
    }
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Koushik0802/MyRepo.git'
            }
        }

        }

    }

